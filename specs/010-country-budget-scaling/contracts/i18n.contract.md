# Contract: i18n Keys (EN + zh-TW)

**Feature**: 010-country-budget-scaling
**Owner**: Frontend Engineer.
**Enforcement**: Constitution Principle VII (NON-NEGOTIABLE) — every new visible string ships in BOTH languages in the same commit, wired via `data-i18n` / `t()`, and mirrored into `FIRE-Dashboard Translation Catalog.md`.

---

## New keys (4) — scaling indicator and tooltip

| Key | EN | zh-TW |
|-----|----|----|
| `geo.scale.line1` | `Country budget: {0} adult{1} → {2}× couple baseline` | `國家預算：{0} 位成人 → {2}× 夫妻基準` |
| `geo.scale.line2` | `+ per-child allowance during pre-college years ({0} children tracked)` | `+ 大學前每位子女津貼（已計入 {0} 位子女）` |
| `geo.scale.tooltip` | `Country budget uses an adults-only factor (OECD-modified: solo ≈ 0.67× couple). Children don't move this factor. Post-FIRE, each child adds an age-graded allowance: \$2,000/yr from ages 0–12, ramping to \$6,000/yr at age 17. College tuition is tracked separately per child.` | `國家預算採用僅計成人的係數（OECD 修正：單人約為夫妻的 0.67 倍）。子女不影響此係數。退休後每位子女依年齡加計津貼：0–12 歲每年 \$2,000，至 17 歲漸升至每年 \$6,000。大學學費則依每位子女單獨計算。` |
| `geo.scale.childrenTracked` | `{0} children tracked` | `已計入 {0} 位子女` |

### Placeholder semantics (Line 1)

- `{0}` — the integer `adultCount` (1 or 2).
- `{1}` — the English plural suffix (`''` when adultCount is 1, `'s'` when adultCount is 2). In zh-TW this placeholder is consumed but rendered empty (Chinese has no plural suffix). The template uses `{0} 位成人` — no `{1}` needed in zh-TW.
- `{2}` — the factor formatted to 2 decimals (e.g., `0.67` or `1.00`).

### Placeholder semantics (Line 2)

- `{0}` — integer count of entries in `childrenList`. Line 2 is rendered ONLY when this count ≥ 1.

### Pluralisation handling

Since English has a simple plural and zh-TW has none, use the existing `t()` helper's positional-placeholder convention rather than a CLDR plural library. Compute the suffix in the render site:

```js
const pluralS = (inp.adultCount === 1) ? '' : 's';
const factorStr = getAdultsOnlyFactor(inp.adultCount).toFixed(2);
indicatorLine1.textContent = t('geo.scale.line1', inp.adultCount, pluralS, factorStr);

if (childrenList.length >= 1) {
  indicatorLine2.textContent = t('geo.scale.line2', childrenList.length);
  indicatorLine2.style.display = '';
} else {
  indicatorLine2.style.display = 'none';
}
```

The zh-TW string ignores `{1}` naturally (no `{1}` occurrence in the template).

---

## Re-used existing keys (NO new catalog entries needed)

These keys already exist at `FIRE-Dashboard-Generic.html` lines 4312–4313 (EN) and 5158–5159 (zh-TW). This feature wires them to the new Adjust Annual Spend `<input>` (see `adjust-annual-spend.contract.md`).

| Key | EN | zh-TW |
|-----|----|----|
| `geo.adjustSpend` | `💰 Adjust Annual Spend:` | `💰 調整年度花費：` |
| `geo.adjustNote` | `(overrides lifestyle preset for this country)` | `（覆蓋此國家的生活方式預設）` |

**No catalog change** required for these two keys — they already exist and are functional; the feature simply uses them.

---

## Acronym policy

Per constitution Principle VII Exemption list, these acronyms stay English globally: `FIRE`, `OECD`, `USD` (via `$`), `SS` (Social Security). All surrounding prose translates. The tooltip text above follows this: "OECD 修正" keeps the `OECD` token in Latin characters, surrounding explanation is translated.

---

## Catalog sync

Add the four new keys, both EN and zh-TW values, to `FIRE-Dashboard Translation Catalog.md` under a new heading `## Feature 010 — Country budget scaling` in the same commit as the code changes.

Example diff block for the catalog:

```markdown
## Feature 010 — Country budget scaling

| Key | EN | zh-TW |
|-----|----|----|
| `geo.scale.line1` | Country budget: {0} adult{1} → {2}× couple baseline | 國家預算：{0} 位成人 → {2}× 夫妻基準 |
| `geo.scale.line2` | + per-child allowance during pre-college years ({0} children tracked) | + 大學前每位子女津貼（已計入 {0} 位子女） |
| `geo.scale.tooltip` | (full tooltip text — see spec) | (full tooltip text — see spec) |
| `geo.scale.childrenTracked` | {0} children tracked | 已計入 {0} 位子女 |
```

---

## Merge gate (Manager check)

Before closing the Frontend task:

1. Grep the changed HTML for the indicator block. Every user-visible word in both lines and the tooltip MUST go through `t()` — no inline English literals.
2. Confirm both `TRANSLATIONS.en['geo.scale.line1']` and `TRANSLATIONS.zh['geo.scale.line1']` exist (and likewise for the other three keys).
3. Toggle the language button at runtime. All four strings MUST flip in place without reload.
4. Confirm `FIRE-Dashboard Translation Catalog.md` has the new heading + 4 rows.
